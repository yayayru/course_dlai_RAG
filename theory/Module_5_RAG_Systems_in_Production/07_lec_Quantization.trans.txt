0:00
Once you're able to evaluate your RAG system and experiment with different configurations,
0:04
you'll be ready to take on some familiar trade-offs in many software projects. Cost, speed,
0:11
and quality. You'll explore these trade-offs over the next few videos, but first, let's introduce
0:18
an important concept, quantization. In short, quantization is compression for both LLMs and
0:25
vectors generated by embedding models. Quantization replaces the model weights inside an LLM or the
0:32
values of an embedding vector with a compressed, lower-precision data type. This makes the models
0:39
or vectors respectively smaller, cheaper, and faster to run, often without much sacrifice
0:45
in retrieval relevance or response quality. Let's have a look to see how it works.
0:50
A good analogy for quantization is image compression. This high-quality image uses 24
0:57
bits of data to represent the color at each pixel. The colors look great, but it does use quite a lot
1:04
of data to store all that information. You can compress the image by using 12 or even 6 bits
1:09
for each pixel. The compressed images are one-half and one-quarter of the size of the original,
1:16
which is a significant memory saving. That said, the 12-bit image already doesn't look as nice,
1:22
and in the 6-bit image, there are many visible color artifacts. Depending on where you're using
1:27
the image, however, this drop in quality might be worth the substantial memory savings.
1:32
Quantization takes the same approach to LLMs and embedding vectors, shrinking their size with some
1:39
loss in quality as a trade-off. Let's start by looking at how it works for LLMs. The parameters
1:45
in a typical language model use 16 bits of memory each. With modern models ranging between roughly
1:52
a billion to a trillion parameters, these models are huge, requiring lots of memory to store them
1:58
and powerful GPUs to run them. Quantized models compress those 16-bit parameters down to 8 or
2:04
even 4-bit equivalents. This significantly reduces the GPU memory required to run the model at the
2:10
cost of a little model performance and quality. Quantized embedding vectors work similarly. A
2:15
fairly typical 768-dimensional vector will use 768 32-bit floating point numbers. That's 3 kilobytes
2:24
of data for every vector in your knowledge base. Higher dimensional models can easily require many
2:29
times that much. Once you're storing millions or even billions of these vectors, you've got huge
2:35
amounts of vector data to deal with. These vectors need to be stored somewhere to use them, and
2:39
especially if you want to use them in rapid vector search, they need to be loaded into expensive RAM.
2:45
Integer quantization is a commonly used approach for shrinking these vectors. It replaces the 32-bit
2:51
floating point numbers with a much smaller integer, for example, an 8-bit integer. This means that your
2:57
vectors are now immediately one quarter of their original size, a massive space saving. These
3:03
integer values are also remarkably easy to calculate. Here's how the process works. You find
3:08
the minimum and maximum values that appear in each dimension for your vector data. This will define
3:14
the range that values in that dimension will fall between. Then, you divide that range into 256
3:21
equally sized sections, the amount of unique values you can make with 8 bits. Those sections
3:26
are then numbered 0, 1, 2, 3, and so on up to 255. Each floating point number from your original
3:34
vectors are then just assigned an integer value equal to whatever section it falls within. If you
3:40
also store the minimum value and the width of each section, you have all the information you need to
3:45
calculate an approximation of the original 32-bit float, but using only 8 bits of data. Despite using
3:53
only a quarter of the data and a seemingly naive compression algorithm, 8-bit integer quantization
3:59
performs remarkably well. For benchmarks like Recall at K, you might only see a drop of a few
4:05
percentage points with 8-bit quantization. These quantized embedding vectors, meanwhile, allow for
4:11
less data to be stored in your vector database, while powering faster search as the calculations
4:16
required have been simplified. Quantized LLMs also often have only minor drops in performance
4:22
when measured using commonly referenced benchmarks. Meanwhile, they use less GPU memory and can
4:28
generate text more rapidly. In other words, these are huge memory and performance gains for a small
4:35
drop in quality. While 8-bit integer quantized vectors are commonly used, 1-bit or binary
4:41
quantized vectors are also gaining popularity. This approach compresses the size of a vector
4:46
by a factor of 32, from 32 bits per dimension to just 1 bit, which is a massive saving. At this
4:54
level of compression, each value in your vector is either a 1 or a 0, and just tells you if that
5:01
value at that dimension was a positive or a negative number. As you might imagine, at these
5:07
extreme compression levels, performance can drop noticeably for embedding model-based retrieval.
5:12
That said, 1-bit quantization results in significantly smaller and faster vector-based
5:17
retrieval. It can also be paired with other techniques. For example, performing fast retrieval
5:23
based on a 1-bit quantized embedding model, and then rescoring using the full original 32-bit
5:30
vector. Another way to shrink the size of your vectors is to use a Matryoshka embedding model,
5:35
based on the Russian word for nesting dolls. These vectors are designed so that you can choose to use
5:41
only a subset of a vector's dimensions when doing things like comparing similarity. For example,
5:47
if the full vector has a thousand dimensions, you could choose to use only the first 500 or the first
5:54
100 dimensions. To enable this behavior, Matryoshka embedding models have a special property.
6:00
Their dimensions are sorted by how information dense they are. Here, information means how much
6:06
statistical variance you'd expect to see in that dimension when embedding large amounts of text.
6:13
In a typical embedding model, every dimension would have roughly the same amount of variance
6:19
or information. In a Matryoshka model, thanks to the training process used, earlier dimensions
6:25
will have more variance, which means more information content. Later dimensions have
6:30
less variance, meaning they provide relatively less information, and so you pay less of a penalty
6:36
for excluding them. There's a few ways to use Matryoshka vectors. You could choose to only ever
6:43
use the first 100 dimensions, saving space and leading to faster computations, while preserving
6:48
the maximum possible amount of information. Or, you could always perform initial retrieval using
6:54
the first 100 dimensions, then pull the remaining 900, now using the full 1000 dimensions, out of
7:01
slower, cheaper memory, to essentially help rescore the initial set of documents you retrieved.
7:07
The flexible properties of Matryoshka models make it best suited for dynamic environments
7:12
where you may want to quickly switch from low to high fidelity vector representations.
7:17
While these advanced techniques show what's possible at the cutting edge of quantization,
7:23
the core takeaway is that you probably should experiment with using integer quantized LLMs
7:29
and embedding models. Most LLM and embedding model providers will make eight or four bit
7:36
quantized models available alongside their base models. The space and cost savings they
7:42
provide can be significant, and the quality reductions are quite small.